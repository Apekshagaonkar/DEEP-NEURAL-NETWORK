install tabula -to convert pdf to csv
->pip install tabula-py
java environment should be set.

install pandas to access the tabular column
import os modules to create files
import shutil to transfer file 

Set the path to Final folder
and start executing code.

BE Carefull about the current working directory 
->os.getcwd()

the commented code lines are the ones which is already used to do some modification which is already done
-creating CLEANED_DATA file
-changing the names of images 
-folders for each location

To apply this on new dataset :
1.download the dataset and keep it in the assignment folder in the same format.
2.uncomment all the lines
3.delete the CLEANED_DATA folder
